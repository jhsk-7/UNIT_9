1) git branch <branch_name>

2) Fast-forward - when the target branch has not changed since the 
      source branch
   Recursive - when combining changes from two branches.

3) git checkout <other_branch_name>

4) git branch -D
   git push -u origin --delete <Old_branch_name>

5) When git can not what file to choose when merging when there have
   been different commits with changes to the same file.
